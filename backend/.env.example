# Squish API environment config
# Copy this to .env and fill in values before deploying.

# Comma-separated list of allowed frontend origins (no trailing slash)
# Dev default is set in main.py: http://localhost:5173,http://localhost:4173
ALLOWED_ORIGINS=https://your-squish-site.vercel.app
